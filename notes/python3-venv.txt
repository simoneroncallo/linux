# python3-venv
sudo <pckg-manager> install python3-venv # Install

python3 -m venv ./path/<name> # Create environment
source ./path/<name>/bin/activate # Activate

pip install --upgrade pip # Update
pip install -r <filename> # Packages

deactivate 
rm -rf ./path/<name> # Remove
